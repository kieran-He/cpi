# 协作约定

## 文件边界

- 原始题目和数据：`questions/`，只读。
- 官方说明和模板：`docs/`、`HuaweiCup23_LaTeX_Template/`，除非明确需要，不直接修改。
- 建模产物：根目录 `题目分析报告.md`、`术语表格.md` 与 `analysis/`。
- 编程产物：`src/`、`scripts/`、`results/`、`figures/`。
- 论文产物：`paper/` 和官方要求的最终导出文件。

## 提交前检查

```powershell
.\scripts\check_repo.ps1
```

有模型或代码变更时，同时在提交说明中记录：

- 修改的子问题和模型版本；
- 是否改变输入、参数、随机种子或输出格式；
- 运行命令和退出码；
- 是否需要重新生成结果、图表或论文。

## 不要提交的内容

个人绝对路径、临时缓存、编辑器配置、未核对的手工结果和覆盖原始附件的清洗文件。可复现的正式结果、图表和复现清单应提交到仓库。
