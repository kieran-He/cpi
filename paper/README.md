# 论文工作区

本目录保存论文大纲、章节草稿和版本快照。正文中的数字、图表和结论只能来自 `results/` 与 `figures/` 的已核对产物。

建议：

- `outline/`：论文结构和主张—证据映射；
- `draft/`：章节草稿；
- `checkpoints/`：可回滚的稳定版本；
- `references/`：经核验的参考文献记录。

已有模板位于根目录 `HuaweiCup23_LaTeX_Template/`，当前只作为候选参考。比赛规则确认后再复制/初始化正式论文项目，不要直接覆盖原模板。
