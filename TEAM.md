# 双人协作分工

## 角色

| 角色 | 主责 | 主要目录 | 交付接口 |
|---|---|---|---|
| 负责人 A：建模/论文 | 读题、拆题、假设、模型选择、公式、论文结构与文字 | `题目分析报告.md`、`术语表格.md`、`analysis/`、`paper/` | `analysis/model_contract.md` 标记为 `READY` |
| 负责人 B：数据/编程 | 数据读取与清洗、模型实现、求解、实验、图表、复现 | `src/`、`scripts/`、`results/`、`figures/` | 结果表、图表和复现命令 |
| 共同 | 官方规则核验、交叉复核、最终提交包 | `docs/`、根目录最终产物 | 双人签字/确认 |

## 交接规则

1. A 先完成模型合同：每个子问题必须有输入、输出、符号、单位、约束、算法和验证方法。
2. B 只实现合同中已确认的内容；发现公式、约束或单位不可实现时，回到 A 修改合同，不在代码里静默猜测。
3. B 交付后，A 核对“代码实现—结果表—论文公式”三者一致；B 再复核论文中的关键数值来自真实结果。
4. 论文写作使用稳定的结果快照。结果或模型发生实质变化时，论文引用必须重新检查。

## 推荐分支

不强制创建分支；若两人并行开发，建议使用：

- `person-a/modeling-<topic>`
- `person-b/implementation-<topic>`
- `integration/<milestone>`

合并到主分支前，提交信息应说明阶段和影响范围，例如 `model: freeze q1 assumptions`、`code: add q2 solver`、`paper: sync result table`。
