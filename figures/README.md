# `figures/` 图表目录

图表文件直接放在本目录，命名遵循：

- `raw_qN_*`：原始数据/数据质量图；
- `process_qN_*`：模型运行过程、收敛、敏感性或验证图；
- `result_qN_*`：最终结果图；
- `flow_overall_model.*`：总体建模流程图；
- `flow_qN_model.*`：存在独立分支或复杂链路时的子问题流程图。

同一逻辑图可导出 SVG 和至少 300 DPI PNG。正式图必须来自代码，不能直接在位图上手工改数字或图例。
